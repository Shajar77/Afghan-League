# Project Rules

## Mobile-First Design
- All components and layouts in this project must follow a **mobile-first design** approach.
- Base CSS styles should target small screens (mobile phones) by default.
- Use `@media (min-width: ...)` queries to scale layouts up progressively for tablets and desktops.
- Ensure touch targets are at least 44px–48px for interactive elements.
